# Geist design-system update log

## 2026-08-24
* **Update**: Migrated to ODSF v0.2 / OKF v0.2. Container: `timestamp` became `generated: { by, at }` across all 86 concepts, eight `# Citations` sections became `sources` frontmatter (10 entries), and the root `index.md` now declares `odsf_version: "0.2"` / `okf_version: "0.2"`. Structure layer: added `styles/wireframe.css` and derived a `*.wireframe.html` for 64 of the 70 components plus the dashboard pattern, each carrying its example's `<body>` verbatim with the skin stripper linked last; wrote a `# Structure` section into all 65 and declared the wireframes in `examples` frontmatter. Six components got none because their examples have no internal nesting at all (badge, checkbox, error, keyboard-input, status-dot, textarea are galleries of leaf variants). Documenting the dashboard skeleton surfaced that its grid collapses at a bespoke `860px` rather than a `{breakpoints.*}` token; recorded in the pattern's `# Structure` with `{breakpoints.lg}` named as the nearest scale value, rather than silently re-tokenizing a working layout.

## 2026-06-23
* **Creation**: Initial Geist (Vercel) bundle for ODSF v0.1 — comprehensive, full public coverage. Tokens read directly from the live vercel.com/geist DOM (`@vercel/geist`) in **both light and dark**, shipping Geist's own `--ds-*` variable names. Includes seven foundations (color scales, typography, grid, shape, elevation, materials, icons), **all 70 documented components** with runnable examples across actions, forms, status & feedback, navigation, overlays, data & content, and device/media frames; the dashboard pattern; theming and focus behaviors; two guidelines with counter-examples (use-the-scale, color-not-alone); three references (docs, live verification, brand assets); and `styles/tokens.css` (light + dark + auto) plus `styles/components.css`.
