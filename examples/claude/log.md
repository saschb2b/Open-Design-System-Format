# Claude design-system update log

## 2026-08-24
* **Update**: Migrated to ODSF v0.2 / OKF v0.2. Container: `timestamp` became `generated: { by, at }` across all 25 concepts, seven `# Citations` sections became `sources` frontmatter (13 entries), and the root `index.md` now declares `odsf_version: "0.2"` / `okf_version: "0.2"`. Structure layer: added `styles/wireframe.css` and derived a `*.wireframe.html` for all eight non-trivial components and the landing-page pattern (badge is a gallery of leaf variants with no internal nesting, so it has none), each carrying its example's `<body>` verbatim with the skin stripper linked last; wrote a `# Structure` section into all nine, declared the wireframes in `examples` frontmatter and the `# Examples` lists. Authoring the top-nav skeleton surfaced a gap: `# Variants & States` describes a hamburger below `{breakpoints.tablet}`, but `components.css` only hides `.top-nav__menu` and no trigger exists in the markup, so the narrow wireframe shows a bar with nothing between the wordmark and the buttons. Documented as-is rather than papering over it.

## 2026-06-23
* **Creation**: Initial Claude (Anthropic marketing) bundle for ODSF v0.1. Authored from a design.md brief and cross-checked against the live claude.com DOM (canvas #faf9f5 and ink #141413 verified exactly; serif-display / humanist-sans split confirmed via `anthropicSerif` / `anthropicSans`; live CTA coral sampled at #c6613f vs. the documented #cc785c). Includes foundations (color, typography, spacing, elevation, shape, motion, layout), nine components with runnable examples (button, top-nav, feature-card, code-window-card, pricing-tier-card, callout-card-coral, badge, text-input, footer), the landing-page pattern, surface-rhythm and press-states-only behaviors, three guidelines with counter-examples, two references, and the runnable `styles/tokens.css` + `styles/components.css`.
